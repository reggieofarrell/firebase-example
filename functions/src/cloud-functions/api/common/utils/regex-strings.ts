/**
 * Regex strings used for validating
 * api route params.
 * e.g. `/:id(${uuidRegexString})`
 */

export const uuidRegexString = '[a-zA-Z0-9- ]+';
export const numberRegexString = '\\d+';
export const alphanumericRegexString = '[a-zA-Z0-9]+';
